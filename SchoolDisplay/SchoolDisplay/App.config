<?xml version="1.0" encoding="utf-8" ?>
<configuration>
  <startup>
    <supportedRuntime version="v4.0" sku=".NETFramework,Version=v4.8" />
  </startup>
  <appSettings>
    <!-- A directory containing multiple PDF files that will show up on this display. -->
    <!-- Simply update its contents to change the current announcements. -->
    <add key="PdfDirectoryPath" value="C:\set\your\path\here" />

    <!-- TODO: documentation missing. Increasing this value slows down scolling. -->
    <!-- TODO: The fastest setting "1" is awfully slow. -->
    <add key="ScrollSpeed" value="1"/>

    <!-- The amount of ms the program waits before jumping to the top again -->
    <add key="PauseTime" value="2500"/>
    
    <!-- TODO: EmptyPollingInterval -->
  </appSettings>
</configuration>